<%= simple_form_for @colegio, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :nombre %>
  <%= f.input :direccion %>
  <%= f.input :telefono %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                colegios_path, :class => 'btn' %>
  </div>
<% end %>
